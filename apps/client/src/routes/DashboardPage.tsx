import { PagePlaceholder } from '../components/PagePlaceholder';

export function DashboardPage() {
  return (
    <PagePlaceholder title="Dashboard" phase="Phase 7 — Dashboard">
      Volume, category mix, response and resolution times, and the AI adoption panel — acceptance
      rate, edit rate, and drafts withheld for lack of grounding.
    </PagePlaceholder>
  );
}
