# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is in its initial state. As of this writing it contains only a placeholder `README.md` — there is no source code, build tooling, dependency manifest, or test suite yet.

**An AI-powered customer support system** that uses AI to answer customer questions and resolve issues (per `README.md`).

## For future instances

There is nothing to build, lint, run, or test yet. As the codebase takes shape, update this file with:

- **Commands** — how to build, run, lint, and test (including running a single test).
- **Architecture** — the big-picture design that spans multiple files: how requests flow through the system, where the AI/LLM integration lives, and how components fit together.

When adding AI/LLM functionality, default to the latest Claude models (e.g. Opus 4.8). See the `claude-api` skill for current model IDs, pricing, and SDK usage before writing integration code.
